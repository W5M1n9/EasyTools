1.NmapResultExtraction is a lightweight tool that extracts Nmap scan results, converts and generates Excel files from them.

2.PingIpChecker is a lightweight tool that performs batch ping tests on IP addresses to verify network connectivity.

3.FscanMultiProcessProcessing is a tool based on fscan, designed to split the long-running fscan scanning process into multiple processes.
